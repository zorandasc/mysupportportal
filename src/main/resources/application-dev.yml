server:
  port: 8081

jwt:
  secret: 'kurecpalemacecjebalec1234567890$#!'

email:
  username: "GMAIL_USERNAME"
  password: "GMAIL_PASSWORD"

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/supportportal
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
